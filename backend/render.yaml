services:
  - type: web
    name: learn-ai-backend
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ENABLE_VIDEO_RENDERING
        value: false
